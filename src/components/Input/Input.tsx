import React from 'react';
import { InputBaseProps } from '@mui/material';
import { StyledInputBase } from './Input.styles';

export const Input = (props: InputBaseProps) => <StyledInputBase {...props} />;
